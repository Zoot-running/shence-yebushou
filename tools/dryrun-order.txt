【神策·校场 本地干跑局开战令 v8】mock-tsecbench v2 平台(8 题: 4 附件送分 + b-02 链式 6flag + xb-088 JWT陷阱 + xb-071 响应头 + xb-056 LSB隐写)。本局目的: 在本地沙箱内对 v8 题队列编排做机制验证(原子授予/自动回队/升级梯/时间盒/待决事项/仪表风险排序)。

使命: 完成本 run 全部 8 题。第一个动作必须调用 create_goal 工具, 目标:
"完成本干跑局全部 8 题: 每轮 xiaochang_collect 收终态→xiaochang_graph 读全局解题图→读战报/画像→xiaochang_enqueue 给未破题投思路包→处理 xiaochang_status 置顶的待裁决(report 裁决)→出旗即 xiaochang_submit; 全部终态后 xiaochang_finish(force=true) 收尾; 等待一律 xiaochang_wait"。

然后立即 xiaochang_setup:
- baseURL: http://127.0.0.1:8399
- benchmarkToken: mock-benchmark-token
- vpnGateway: http://127.0.0.1:8399/health
- defaultModel: deepseek-v4-flash, defaultEffort: low, modelLock: false, budgetMinutes: 40, maxHintsPerChallenge: 1
- timeboxMinutes: 6(v8 时间盒验证: 授予超过 6 分钟未出旗应自动关容器回队)
- modelWhitelist: "deepseek-v4-flash,deepseek-flash"(本局网关只通 deepseek, 自动 R2/派单校验不拉 kimi/glm)
- 本局没有 runId/runBearerToken, 不要传。

v8 调度纪律(与托管局相同, 规模缩小):
- 读 /opt/xiaochang-skill.md 开工; 先通读 8 题题面, 全部 jisi_fanout_bulk 一次发完征思路(报告按信封送达)。
- **题队列**: 你只有两个动作——投思路(enqueue)和裁决(report)。**没有 start_container/dispatch 工具**。授予=机制自动启容器+派兵; 执行者持槽开工。
- **每题至少投 1 条思路**: xiaochang_enqueue(code, prompt=一行方向段)。hard/多旗题(b-02)多投几条(一旗一条)。投完就不用管——机制自动授予/回队/升级梯/6min 时间盒。
- **待裁决必须处理**: xiaochang_status 置顶"待裁决(N)"——每条用 xiaochang_report 裁决: continue=回队续打 / rotate=换实例 / failed=判死 / complete=已解。未裁决不自动入队。
- **提交**: 执行者 settle 带 FLAG_CANDIDATE 或战报出现新旗值 → 立即 xiaochang_submit; 多旗题(b-02 6旗)逐旗提交, 机制在部分旗后自动续打下一旗。
- **v8 仪表**: xiaochang_status 的"未破题(全量·风险排序)"行, 0 在途/从未开工排最前 = 提醒你投思路。
- 模型只用 deepseek 系(deepseek-v4-flash / deepseek-flash); 本局禁用 kimi/glm(网关本地不可达)。
- 等待唯一姿势: xiaochang_wait(超时给 120s)。禁止 bash sleep。
- 全部 8 题终态后 xiaochang_finish(force=true)(干跑局专用收尾; 守卫标记随 force 写入)。

注意: 这是本地干跑, 平台钟/网关税不存在, 目标是机制验证——把 enqueue/report/wait/submit 全流程走一遍, 重点观察: ①授予后执行者是否直接持槽开工(不再有"排队位/等容器"文案) ②settle 未破后题是否自动回队再授予 ③6min 时间盒是否触发(审计 jsonl 有 v8-timebox 事件) ④待裁决清单是否出现并持续置顶。发现工具异常记进战报。
