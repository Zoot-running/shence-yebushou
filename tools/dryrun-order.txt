【神策·校场 本地干跑局开战令】mock-tsecbench v2 平台(8 题: 4 送分 + b-02 链式 6flag + xb-088 JWT陷阱 + xb-071 响应头 + xb-056 LSB隐写)。本局目的: 在本地沙箱内对完整插件链做机制验证(F33 分叉信箱/结构化报告/全局解题图/能力账本种子)。

使命: 完成本 run 全部 8 题。第一个动作必须调用 create_goal 工具, 目标:
"完成本干跑局全部 8 题: 每轮 xiaochang_collect 收终态→xiaochang_graph 读全局解题图→读战报/画像/能力账本→判断→xiaochang_enqueue 派最合适的执行者→xiaochang_dispatch; 等待一律用 xiaochang_wait; 任一题拿齐 flag 即 xiaochang_submit+xiaochang_report(complete, 带 deadEnds/forks/observations); 全部终态后 xiaochang_finish(force=true) 收尾"。

然后立即 xiaochang_setup:
- baseURL: http://127.0.0.1:8399
- benchmarkToken: mock-benchmark-token
- vpnGateway: http://127.0.0.1:8399/health
- defaultModel: deepseek-v4-flash, defaultEffort: low, modelLock: false, budgetMinutes: 40, maxHintsPerChallenge: 1
- modelWhitelist: "deepseek-v4-flash,deepseek-flash"(v7.8: 本局网关只通 deepseek, 自动 R2/派单校验不拉 kimi/glm)
- 本局没有 runId/runBearerToken, 不要传。

调度纪律(与托管局相同, 规模缩小):
- 读 /opt/xiaochang-skill.md 开工; 先通读 8 题题面, 全部 jisi_fanout_bulk 一次发完征思路(easy 1 个便宜模型即可, 报告按信封送达)。
- 批量发兵: 能并行的工具调用在同一条消息里一次发完。每题先派 1 兵(easy/medium 单兵, hard 双兵两路思路)。
- **v7 极简执行令**: prompt 只写一行指令(指派思路+验证点), 不抄题面/入口/纪律——机制自动包框架。每题第一批兵派单前, 先 xiaochang_knowledge_put 写 ① 题源思路骨架(出处+骨架步骤)。
- **v7 知识账本**: 执行者开工先读 boards/<runId>/<code>/KNOWLEDGE.md; 重派只写"按账本①第 N 条思路继续/换方向"; 死路/分叉/工件由 report/fork 自动累积(②③④)。
- **v7 资源类**: g-m1..4 是附件题(题面"无需容器")→ 第 0 分钟全并行, 不占容器槽; b-02/xb-088/xb-071/xb-056 是容器题 → 最多 3 在途轮换(xiaochang_status 看 resourceClasses 行)。容器题执行需要 addrs 时先 xiaochang_start_container。
- 模型只用 deepseek 系(deepseek-v4-flash / deepseek-flash); 本局禁用 kimi/glm(网关本地不可达)。
- **多 flag 题(b-02)**: 按 flag 拆工作项——一个 flag 一个执行者, 后一 flag 用 dependsOn 依赖前一 flag 的 item id; flag 全齐才 report(complete)。
- **v6 判死/升级纪律**: xiaochang_status 出现 ⚠️(升级建议)→ 执行 xiaochang_refanout; 出现 ⛔(判死建议)→ 核对理由后 report(failed, why=approach-dead-end); 出现"末段自动 R2"通知 → 报告到达后照常 jisi_adjudicate 裁决。
- 等待唯一姿势: xiaochang_wait(超时给 120s)。禁止 bash sleep。
- v2 纪律(必须走): ①fanout 带 qtype/difficulty(jisi_pick 先看双维度候选); ②报告到达即 jisi_adjudicate 批量裁决(adopted/not-adopted/pending); ③report 必填 why(失败时)+gaps(缺上下文时); ④xiaochang_status 出现 ⚠️ 升级建议 → xiaochang_refanout; ⑤每轮看 jisi_model_report(v2 后验/效费比)。
- 全部 8 题终态后 xiaochang_finish(force=true)(干跑局专用收尾; 守卫标记随 force 写入)。

注意: 这是本地干跑, 平台钟/网关税不存在, 目标是机制验证而不是速度——把 fork/report/graph/enqueue 附情报全部走一遍, 发现工具异常记进战报。
